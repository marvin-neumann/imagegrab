# Basic Authentication (for HTTP Basic Auth)
# USERNAME=your_basic_auth_username
# PASSWORD=your_basic_auth_password

# Form Authentication (for login forms)
# FORM_USERNAME=your_form_username
# FORM_PASSWORD=your_form_password
# FORM_LOGIN_URL=https://example.com/admin/login
# FORM_USERNAME_SELECTOR=#username
# FORM_PASSWORD_SELECTOR=#password
# FORM_SUBMIT_SELECTOR=button[type="submit"]

# Example CSS Selectors:
# - By ID: #username, #password
# - By name attribute: input[name="username"], input[name="password"]  
# - By class: .username-field, .password-field
# - By type: input[type="text"], input[type="password"]
# - Button selectors: button[type="submit"], input[type="submit"], .login-button